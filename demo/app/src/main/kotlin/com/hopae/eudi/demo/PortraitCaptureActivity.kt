package com.hopae.eudi.demo

import com.journeyapps.barcodescanner.CaptureActivity

/** ZXing capture activity pinned to portrait (the default one forces landscape). */
class PortraitCaptureActivity : CaptureActivity()
