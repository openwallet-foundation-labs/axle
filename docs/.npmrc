# Supply-chain guard: never resolve a version published within the last 7 days,
# so a compromised release has time to be reported and yanked before we pull it
# in. 10080 = minutes. pnpm >= 10.16 only; npm has no equivalent setting, which
# is why every JS project here is on pnpm.
minimum-release-age=10080
